# Training checkpoints (LoRA adapter weights + optimizer state)
output/**/final/
output/**/checkpoint-*/

# Raw tinygsm jsonls — gzipped copies (.jsonl.gz) are committed alongside.
# Run `gunzip -k data/tinygsm/*.jsonl.gz` on a fresh clone before training.
data/tinygsm/*.jsonl
